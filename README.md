# 落点 · 四川 2026 高考志愿查询

静态站点，打开即用，不需要服务器、不需要安装任何东西。

**第二代：<https://yusheng266186-beep.github.io/sichuan-gaokao-2026/v2/>**

**课堂演示：<https://yusheng266186-beep.github.io/sichuan-gaokao-2026/v2/?stage=1>**

第一代保留在 <https://yusheng266186-beep.github.io/sichuan-gaokao-2026/>。

## 第二代 · 每一种可能，都值得被看见

围绕「定位 → 探索 → 了解 → 收藏与比较」重新设计，采用浅绿底色、清晰的信息层级和统一的克制动效。

- 三种浏览入口：专业组、院校、专业；支持关键词、多条件交叉筛选、位次分档和排序。
- 分数刻度可拖动；分数与位次双输入；再选科目核验；支持低分、缺失数据和表格边界提示。
- 组线、专业录取线、学费与专业评价分层展示，明确年份与来源；缺少往年线不自动称为新增组。
- 心愿单按稳定的专业组标识保存，支持上下排序、文本导出、跨标签页同步；一次可并排比较 2–3 个组。
- 白板模式放大文字、按钮、结果卡片和详情，提供全屏入口与底部操作栏；手机采用单列布局。
- 初始检索目录约 2.77 MB，较原始四份数据合计约 12.77 MB 缩小约 78%（未压缩体积）；专业明细按需加载并校验 SHA-256 与版本。
- 第一代同步修复页面路由与列表页码共用 `state.page` 的问题。

### 数据维护

`data/*.js` 仍是唯一的原始数据入口。更新后运行：

```bash
python tools/build-v2.py
```

将原始数据与更新后的 `v2/catalog.json` 一起提交。无需 npm、后端服务或构建平台即可在 GitHub Pages 运行。

回归检查：`node --test tests/v2-core.test.js`。布局校检入口为 `tools/preview.html`，可切换手机、平板、桌面与白板尺寸。

本地预览可在仓库根目录运行 `python -m http.server 8000`，打开 `/v2/`。

`v2/core.js` 负责纯数据逻辑，`v2/app.js` 负责交互，`v2/style.css` 负责视觉。第一代与第二代收藏分别保存，互不覆盖。

### 第二代定位参数

`?score=580&track=0`：物理类分数定位；`?rank=30000&track=1`：历史类位次定位；`?stage=1`：课堂演示。

筛选条件会写入地址，可复制地址分享相同的查询状态；`#saved` 打开心愿单，心愿单内容仅保存在当前浏览器，不随链接分享。

---

## 这是什么

四川 2026 新高考（3+1+2）的志愿查询工具。以**院校专业组**为志愿单位
（本科批 B 段共 45 个志愿位），帮考生回答三个问题：

1. **我这个位次能填哪些组？** —— 冲 / 稳 / 保分档，分数为主、位次为辅
2. **这个组的线和我差多少？** —— 同口径换算（把你的位次换成上一年的等效分再比）
3. **这些专业值不值得读？** —— 专业级的软科评级、学科评估、硕博点

## 六个页面

| 页面 | 内容 |
|---|---|
| **定位** | 输入分数或位次，选再选科目 |
| **选组** | 12,275 个院校专业组，按冲稳保分档，多面筛选 |
| **专业** | 先选专业再看学校：哪些学校在川招它、你够得着哪些 |
| **院校** | 2,308 所院校，直接显示在川调档线分数区间 |
| **志愿表** | 45 个志愿位、梯度诊断、拖拽排序、导出文本 |
| **标签说明** | 985 / 211 / 保研资格这些标签分别是什么意思 |

## 快捷方式

网址后加参数可以直接跳到指定状态，适合存成书签：

```
?score=600&track=0        直接定位到 600 分 · 物理类
?score=600&track=1        600 分 · 历史类
?score=669&group=51       定位后直接打开某个专业组
?stage=1                  演示模式（全屏 + 字号放大，适合投屏/白板）
?noanim=1                 关闭全部动画
#groups / #major / #schools / #plan / #find
```

## 数据

| 内容 | 规模 |
|---|---|
| 院校 | 2,308 所 |
| 院校专业组 | 12,275 个 |
| 招生专业条目 | 51,878 条 |
| 专业名 | 1,543 个 |
| 录取观测记录 | 264,971 条 |

数据来自四川省教育考试院招生考试报、教育部阳光高考信息公开平台、
各校官方招生章程，以及 2022–2025 年历史录取数据。

## 三条必须知道的口径

**① 2025 与 2026 均为新高考口径，但专业组仍可能变化。**
2024 及以前是**文理分科**，与现在的物理类 / 历史类不是同一定义，
本站不参与分档，只在详情页作为趋势参考。

**② 分数不能直接跨年相减，位次对照也需保留不确定性。**
两年的卷子不同。要看差距，本站会把你的位次换算成上一年的等效分再比
（例：位次 22,303 → 2025 等效 601 分，与组线 572 分相比 +29 分）。

**③ 学费有两个口径，互不覆盖。**
「招生考试报填报」是四川口径、专业级，应结合当年正式收费信息核对；
「官方收费表」是院校级、按收费类别列，可能来自其他省份的招生计划。
两者不一定对口，**本站不做「一致 / 冲突」判定**。

## 免责

本站提供的是**往年录取位置的对照，不构成录取概率预测**。
专业组可能逐年重组，招生计划、批次与投档规则也可能调整。
正式填报请以学校官网、当年招生章程和四川省教育考试院公布的招生计划为准。

---

<sub>页面与数据由本仓库直接托管；`data/*.js` 为构建产物，请勿手工编辑。</sub>
